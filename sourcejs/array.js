// Arrays : they are variables that hold different values
// js allows any data type to be stored in array

let numbers = [1,2,3,4,5];

let names = ['dave','jonah','maggie','honesty'];

// accessing elements of array
// console.log(numbers[4]);
// console.log(names[1]);

//updating array elements

// numbers[0] = 0;
// console.log(numbers);

//  undefined elements 
// console.log(numbers[5]);

numbers.push(77); // add element at the end of the array
//numbers.pop() // removes element at the end of the array

console.log(numbers.length);
console.log(numbers);
